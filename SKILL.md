---
name: 六爻装卦
slug: liuyao-najia
displayName: 六爻装卦（京房纳甲）
summary: 给定六爻阴阳与起卦时间，按京房纳甲法装出完整卦表：八宫、世应、纳甲、六亲、六神、伏神、变卦、旬空、六冲／六合；可解析并逐层校验论坛装卦表。可选接入自备的大模型密钥，做卦象要点梳理。
description: 按京房纳甲法（《增删卜易》体系）装卦。给定六爻阴阳与起卦时间，输出完整卦表——八宫归属与卦名、世爻应爻、纳甲干支与五行、六亲、六神、伏神、变卦（含完整卦形）、旬空、六冲／六合。本卦可用多种写法输入：经卦写法（上离下巽）、六十四卦名或其唯一简称（火风鼎／鼎）、六位阴阳（011101）。可解析论坛标准装卦表文本并逐层校验（宫位、纳甲、六亲三层），也能独立核对既有卦表是否装错。年柱以立春时刻为界、月柱以十二节时刻为界，使用内置 1900-2100 精确节气表；子时归次日。纯本地计算，不联网、不上传数据，只做盘面结构整理。可选接入您自己的大模型密钥做卦象要点梳理，仍不含吉凶断语。
version: 1.4.0
author: 启明咨询
license: MIT
category: [生活实用]
tags: [六爻, 周易, 装卦, 纳甲, 京房, 排盘, 世应, 六亲, 卦象, 纯本地, 大模型可选]
trigger: [六爻, 装卦, 纳甲, 京房, 排盘, 周易, 卦象, 世应, 六亲, 伏神, 变卦, 旬空, 起卦, 卦表校验, 摇卦, 大模型解读, 卦象梳理, 接自己的API]
requires_api_key: false
---

# 六爻装卦（京房纳甲）

把「六爻阴阳 + 起卦时间」整理成结构化的卦表，并核对已有卦表装得对不对。
**只做盘面结构计算，不做吉凶判断。**

## 能做什么

| 功能 | 用法 | 说明 |
|---|---|---|
| 装卦 | `--gua "上离下巽" --dong 6` | 输出八宫、卦名、世应、纳甲、六亲、六神、伏神（标进对应爻行）、变卦（含完整卦形）、旬空、六冲／六合 |
| 多样写法 | `--gua "火风鼎"` / `--gua "上卦为离，下卦为巽"` | 卦名（或其唯一简称）、经卦、倒装、六位阴阳四类写法皆可，见「输入约定」 |
| 解析卦表 | `--parse-text 文件.txt` | 把论坛标准装卦表文本转成结构化数据 |
| 校验卦表 | 随解析自动执行 | 三层校验：宫位自洽、纳甲逐爻、六亲归属 |
| 自检 | `--self-test` | 内置 96 项断言，覆盖卦表唯一性、输入写法、纳甲、六亲、世应、六神、伏神、变卦、旬空、六冲／六合、解析校验、节气与子时，以及可选解读层的网络隔离、密钥掩码与降级行为 |
| 要点梳理（可选） | `--llm` | 接入您自备的大模型密钥，在卦表之后追加一段「卦象要点梳理」。**不带 `--llm` 时完全不启用**：不联网、不加载网络模块、不读配置文件。详见下文专节 |

## 怎么用

```bash
# 装卦（推荐：直接给四柱，最精确）
python3 scripts/liuyao_zhuanggua.py --gua "上离下巽" --dong 6 \
    --ganzhi "丙午 丁酉 戊子 戊午"

# 直接报卦名（含唯一简称），与上面等价——适合手上只有卦名的场合
python3 scripts/liuyao_zhuanggua.py --gua "火风鼎" --dong 6 \
    --ganzhi "丙午 丁酉 戊子 戊午"

# 口语写法亦可，标点与空格随意
python3 scripts/liuyao_zhuanggua.py --gua "上卦为离，下卦为巽" --dong 6 \
    --date "2026-09-14 12:45"

# 用公历时间（年柱月柱按内置精确节气表判定，可精确到分钟）
python3 scripts/liuyao_zhuanggua.py --gua "上震下艮" --dong 1,2,5 --date "2026-09-14 12:45"

# 静卦 / JSON 输出 / 写入文件（Windows 控制台中文乱码时用 --out）
python3 scripts/liuyao_zhuanggua.py --gua "上兑下坎" \
    --ganzhi "丙午 丁酉 戊子 戊午" --json --out result.json

# 用六位阴阳直接指定（自下而上，1=阳 0=阴）
python3 scripts/liuyao_zhuanggua.py --gua-bin 100101 --dong 1 --date "2026-09-14 12:45"

# 解析并校验论坛装卦表文本
python3 scripts/liuyao_zhuanggua.py --parse-text dump.txt

# 自检（96 项）
python3 scripts/liuyao_zhuanggua.py --self-test
```

## 可选：接入您自己的大模型

装卦本身是纯本地计算，不需要任何密钥。若希望在卦表之后另加一段「卦象要点梳理」，可自行接入一个大模型接口。
**这一段是可选的**：不加 `--llm`，程序不联网、不加载网络模块、不读任何配置文件，行为与上一版完全一致。

```bash
# 先体检：看当前配置状态，不发起任何调用，密钥以掩码显示，可放心截图
python3 scripts/liuyao_zhuanggua.py --llm-doctor

# 预演：只打印会发出去的内容，不外发，用于先确认送出去的是什么
python3 scripts/liuyao_zhuanggua.py --gua "火风鼎" --dong 6 \
    --ganzhi "丙午 丁酉 戊子 戊午" --llm-dry-run

# 正式调用：接云接口
python3 scripts/liuyao_zhuanggua.py --gua "火风鼎" --dong 6 \
    --ganzhi "丙午 丁酉 戊子 戊午" --llm --llm-ask "问这桩生意"

# 接本机模型：数据不出本机，无需密钥
python3 scripts/liuyao_zhuanggua.py --gua "火风鼎" --dong 6 \
    --ganzhi "丙午 丁酉 戊子 戊午" --llm --llm-provider ollama --llm-model qwen2.5:7b

# 指定服务商与模型
python3 scripts/liuyao_zhuanggua.py --gua "火风鼎" --ganzhi "丙午 丁酉 戊子 戊午" \
    --llm --llm-provider siliconflow --llm-model "Qwen/Qwen3-8B" --llm-ask "问货物何时到"
```

**输出分三段，彼此物理隔开**：第一段是卦表（本机计算，可逐项复核）；第二段是要点梳理（标注为外部大模型生成，声明非本工具计算结果）；第三段是边界说明。

**配置密钥的三种方式**，优先级由高到低：

| 方式 | 做法 | 生效范围 |
|---|---|---|
| 命令行 | `--llm-key "您的密钥"` | 当次。注意会留在命令历史里，一般不建议 |
| 环境变量 | `LIUYAO_LLM_KEY`（另有 `LIUYAO_LLM_PROVIDER`／`_MODEL`／`_BASE`／`_TIMEOUT`） | 当前会话 |
| 配置文件 | 在 `~/.liuyao/config.json` 写入 `{"provider":"deepseek","model":"deepseek-chat","api_key":"您的密钥"}` | 长期 |

内置服务商（`--llm-provider` 取值，预设接口地址与默认模型，可用 `--llm-base`／`--llm-model` 覆盖）：

| 取值 | 服务商 | 是否需密钥 |
|---|---|---|
| `deepseek`（默认） | 深度求索 | 是 |
| `siliconflow` | 硅基流动 | 是 |
| `dashscope` | 阿里云通义千问 | 是 |
| `moonshot` | 月之暗面 Kimi | 是 |
| `zhipu` | 智谱 GLM | 是 |
| `openai` | OpenAI | 是 |
| `anthropic` | Anthropic Claude | 是 |
| `ollama` | 本机 Ollama | 否（数据不出本机） |

**这一段做什么、不做什么**：只把卦表内部的五行与六亲关系梳理清楚——用神取法、用神与月建日辰的关系、动爻变爻的生克、世应关系、伏神与旬空、需人工核对之处。**仍然不给吉凶结论、不给行动建议、不预测应期**，提示词中对这三类表述作了硬性禁止。梳理结果由外部模型生成，**不可复现，本工具不为其准确性作任何担保**。

**接入外部接口意味着卦表内容会发送到您所指定的服务器**。若不便外发，改用 `--llm-provider ollama` 走本机模型，或只用不带 `--llm` 的纯本地模式。调用产生的费用由您与所选服务商结算，与本工具作者无关。

**密钥只存在内存里**：不写入输出、不写入结果文件、不写入日志，任何展示场合一律以掩码形式出现（如 `sk-ab……XYZ`）。送出内容可先用 `--llm-dry-run` 自行核验。

**失败不会影响卦表**：没有密钥、网络不通、接口报错、超时，卦表照常完整输出，解读段如实写明失败原因与排查步骤，绝不静默、绝不用编造内容填补。

## 输入约定

- **六爻位序**：字符串自下而上，第 1 位是初爻；`1` 为阳爻、`0` 为阴爻。
  例：乾为天 `111111`；火雷噬嗑 `100101`（下震 `100` + 上离 `101`）。
- **本卦写法**（`--gua`，下列写法等价，标点空格随意）：

  | 写法 | 示例 | 说明 |
  |---|---|---|
  | 经卦写法 | `上离下巽` | 先上卦、后下卦；卦名（乾兑离震巽坎艮坤）与别名（天泽火雷风水山地）均可 |
  | 口语写法 | `上卦为离，下卦为巽` | 「卦」「是」「为」等虚词可省可留 |
  | 倒装写法 | `离上巽下` | 与第一种同义 |
  | 卦名写法 | `火风鼎`、`火风鼎卦` | 六十四卦标准名 |
  | 卦名简称 | `鼎`、`噬嗑`、`小过` | 仅限在六十四卦内唯一的简称；重名时不收录，须写全名 |
  | 六位阴阳 | `011101` | 与 `--gua-bin` 等价 |

- **动爻**：`--dong "5"` 或 `--dong "1,2,5"`；留空即静卦；越界（如 7）直接报错。重复输入自动去重。
- **起卦时间**（二选一）：
  - `--ganzhi "丙午 丁酉 辛卯 甲午"` —— **推荐**，四柱直接给定，最精确；
  - `--date "2026-09-14 12:45"` —— 日柱、时柱由引擎推算；年柱以立春时刻为界、月柱以十二节时刻为界，取自内置节气表。

认不出的写法一律明确报错并列出可用写法，不猜测、不将就。

## 输出字段

- `ben_gua`：卦名、所属宫（八宫）、宫内位置（本宫／一世……游魂／归魂）、世爻与应爻位置、上下经卦
- `lines`（初爻 → 六爻）：阴阳、纳甲干支、地支五行、六亲、六神、是否世／应、是否动爻、是否旬空。文本输出的爻表另设「伏神」列，把该爻伏神**直接标在本行**，不必到表外逐行比对
- `fushen`：本卦不现的六亲，取本宫首卦同爻位干支为伏神，标注伏于何爻之下（文本输出在爻表内标出，另于表末汇总一行）
- `chonghe`（本卦与变卦各一）：按纳甲地支判定卦的冲合——初—四、二—五、三—六三对爻位地支**全部相冲**为「六冲卦」，**全部相合**为「六合卦」，其余为空串。依此判据，六冲卦为八纯卦加天雷无妄、雷天大壮共十卦；六合卦为地天泰、天地否、雷地豫、地雷复、火山旅、山火贲、水泽节、泽水困共八卦
- `bian_gua`：变卦**完整卦形**——卦名、宫位、宫内位置、五行、世爻与应爻、上下经卦，以及六爻逐爻的阴阳、纳甲干支、五行、六亲（按本卦宫定）、世应标记、旬空、是否动爻；`changed` 字段另列每个动爻变前变后的干支与六亲。文本输出对变卦单列一表，逐爻与卦形并列，便于对照核对。
- `time`：年、月、日、时四柱与旬空

## 关键口径（易错点）

1. **变爻六亲按本卦宫定**，不按变卦宫——与主流排盘一致。
2. **世应相隔三爻**：世一应四、世二应五、世三应六、世四应初、世五应二、世六应三。
3. **六神按日干起**：甲乙日起青龙、丙丁日起朱雀、戊日起勾陈、己日起螣蛇、庚辛日起白虎、壬癸日起玄武，自初爻依次向上循环。
4. **旬空按日柱所在旬取**（如辛卯日属甲申旬，空午未）。
5. **归魂卦**＝游魂卦的内卦回本宫（等价于只反第五爻）；**本宫（八纯卦）皆为六冲卦**。
6. **年柱以立春时刻为界、月柱以十二节时刻为界**，判据是**时刻**而非日期。例如 2026 年立春在 2 月 4 日 04:01，则当日 03:00 起卦仍属上一年乙巳年、丑月。
7. **子时（23:00—23:59）按通行口径归次日**：日柱推进一日，时柱以次日日干起子时。同一日的 23:30 与次日 00:30，四柱一致。
8. **六冲／六合按纳甲地支现算，不按卦名死记**：取初—四、二—五、三—六三对爻位的地支，三对全冲为六冲卦、三对全合为六合卦。据此八纯卦（本宫卦）皆六冲，与第 5 条一致；地天泰一类上下对应爻位地支两两相合者为六合卦。程序按规则现算，非查表硬编码，判据可逐卦复核。

更完整的规则表（八宫卦序、纳甲表、六亲取法、伏神取法）见 [references/zhuanggua-rules.md](references/zhuanggua-rules.md)，需要人工核对装卦结果或解释取法时再读。

## 精度与校验依据

本工具的正确性经过逐项实测，不是「大概对」：

- **日柱**：与寿星天文历（sxtwl 2.0.7）逐日比对 **1900-01-01 至 2100-12-31 共 73,414 天，零差异**；另经 6tail lunar 库（lunar_python 1.4.8）交叉核对一致。
- **旬空**：同期 73,414 天逐日比对，零差异。
- **节气**：内置 1900-2100 共 2,412 个「节」的**精确时刻**（分钟精度），逐条与 lunar_python 独立比对，偏差不超过 1 分钟，两库切换点偏差最大 0 分钟。
- **年柱／月柱**：14,683 天抽样，另加 2,412 个节气时刻前后 ±2 分钟共 4,824 个边界点，全部与独立参考实现一致。
- **时柱**：2026 全年每日 24 个整点共 8,760 次比对，零差异（含子时归次日）。
- **装卦规则**：另写一份独立实现（从卦名反推上下经卦、以「首支＋阳顺阴逆」规则生成纳甲、以五行关系矩阵生成六亲），对 64 卦 × 六爻全字段比对 260 项，零问题；并与元亨利贞网真实卦表逐爻一致。
- **输入写法**：六十四卦逐个以「标准卦名」与「上X下Y」两路直输，共 128 例全部还原为正确位序；另测同一卦十种写法（含口语、倒装、简称、六位阴阳）结果一致；无法识别的写法与越界动爻一律报错，不猜测。
- **六冲／六合**：按「初—四、二—五、三—六三对爻位纳甲地支全冲／全合」的规则现算，对 64 卦全量判定，所得六冲十卦（八纯卦加天雷无妄、雷天大壮）、六合八卦（地天泰、天地否、雷地豫、地雷复、火山旅、山火贲、水泽节、泽水困）与通行列表逐一吻合。
- **可选解读层**：在本地模拟接口上实测六种情形——OpenAI 兼容格式与 Anthropic 格式（均应正确解析）、密钥无效（HTTP 401）、对端无响应（超时）、返回空内容、接口地址不可达。逐一核对：成功时正确取回内容并标注来源；任何一种失败下卦表均完整无损，且给出可照做的排查步骤；超时按设定秒数截断（实测 5 秒设定对 30 秒拖延，耗时 5 秒）；完整密钥在任何输出、任何结果文件中出现 0 次，一律以掩码显示；预演模式确实不外发。
- **默认路径的网络隔离**：逐次核验不带 `--llm` 时全程不加载 `urllib.request`、`http.client`、`ssl`——网络模块只在 `--llm` 分支内部延迟导入，且此项已写成自检断言，随每次 `--self-test` 复算。
- **内置自检**：96 项断言，运行 `--self-test` 即可复核（含可选解读层的网络隔离、密钥掩码与降级行为）。

超出 1900-2100 年范围时，年柱／月柱退回近似公式推算，程序会在输出中明确标注「为近似值，请核对」。

## 边界与免责

**使用边界**

- **装卦部分只做盘面计算。** 工具负责的是卦象结构的整理与核对：宫位、世应、纳甲、六亲、六神、伏神、变卦、旬空。
- **不做吉凶判断。** 不给预测结论、不给行动建议、不判吉凶。启用 `--llm` 后，可选解读层会梳理财象要点（用神取法、用神与月建日辰的五行关系、动变生克、世应关系、伏神与旬空），这些均属盘面内部关系的陈述，**同样不含吉凶结论、不含建议、不预测应期**。
- **可选解读层默认关闭，不改变本工具的定位。** 开启后由使用者自备密钥、自负费用；该段内容的准确性由所指定的模型负责，本工具不为其作任何担保。
- **不替使用者猜数。** 卦名不在六十四卦标准名内、经卦认不出、动爻越界，一律明确报错，不猜测、不将就。卦名简称一旦在六十四卦内重名即不予收录，此时须写全名。

**免责声明**

1. **本工具属传统术数符号体系的整理工具，其内容不具备科学依据，请理性看待。** 输出的卦表只是对传统排盘规则的程序化实现，不代表任何事实判断或预测能力。
2. **输出仅供传统文化研究与学习参考，不构成任何形式的决策依据。** 请勿依据本工具的结果作出医疗、健康、财务、投资、婚恋、法律或其他重大事项的决定；涉及此类事项，请咨询相应领域的专业人士。
3. **默认不上传数据。** 不启用 `--llm` 时，全部计算在使用者本机完成，输入的卦象与时间不联网、不留存、不回传。启用 `--llm` 后，卦表内容会发送至使用者自行指定的接口；密钥由使用者自备，本工具不代收、不代付、不转发、不留存。
4. **可选解读层的内容不是本工具产出的。** 要点梳理由使用者指定的外部大模型生成，不可复现，本工具不参与、不校验、不为其准确性作任何担保；该部分同样不含吉凶判断。
5. **使用者应自行判断并承担使用后果。** 因使用本工具产生的直接或间接损失，作者不承担责任。
6. **请遵守当地法律法规与公序良俗。** 本工具仅用于个人学习与传统文化研究，不得用于欺诈、敛财等违法用途。

## 文件说明

```
SKILL.md                        本文件
scripts/liuyao_zhuanggua.py     全部功能（纯标准库、无第三方依赖，含 96 项自检；可选解读层亦只用标准库发请求）
references/zhuanggua-rules.md   八宫卦序、纳甲表、六亲与伏神取法、校验口径
agents/openai.yaml              界面显示名与简述
```
